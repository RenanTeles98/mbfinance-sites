# Sessao 2026-07-13

## Resumo

- Reorganizada a area de influencers da aba Videos IA.
- Cada projeto agora tem sua propria lista de influencers.
- Ao selecionar um projeto, aparecem somente as influencers cadastradas naquele projeto.
- A influencer Heena Duarte foi configurada como padrao inicial da MB Negocios com os IDs de aparencia e voz informados pelo usuario.

## Arquivos alterados

- `private/blog-admin.html`
- `public/assets/js/admin/admin-videos.js`
- `CONTEXT.md`
- `TODO.md`
- `DECISIONS.md`
- `CHANGELOG.md`

## Decisoes

- Manter o primeiro cadastro em `localStorage` para validar o fluxo antes de criar banco/API.
- Concentrar manual, referencia visual, ID de aparencia e Voice ID dentro do perfil da influencer.
- Mostrar no lote apenas a influencer selecionada, deixando os IDs tecnicos no cadastro da personagem.

## Validacao

- `node --check public/assets/js/admin/admin-videos.js`
- `npm run build`

## Proximos passos

- Testar em producao a criacao de roteiro com MB Negocios e Heena Duarte.
- Fazer um envio real ao HeyGen para confirmar se o campo atual aceita o ID de aparencia informado.
- Evoluir persistencia para banco/API quando o fluxo for aprovado.

## Ajuste visual do card de influencers

- Corrigido o layout do card Influencers do projeto.
- O formulario interno passou para coluna unica.
- A lista da influencer ativa e os botoes ficaram separados do cadastro.
- Validacao: `npm run build`.

## Foto e manual anexados

- Adicionado upload de foto da influencer, com preview no avatar do card.
- Adicionado upload de manual como documento local.
- TXT/MD/JSON preenchem o resumo usado nos roteiros; PDF/DOC/DOCX ficam anexados ao perfil.
- Validacao: `node --check public/assets/js/admin/admin-videos.js` e `npm run build`.

## PDFs maiores no manual

- Corrigido erro de limite ao anexar PDF no manual da influencer.
- Arquivos PDF/DOC sao armazenados em IndexedDB no navegador.
- Metadados continuam no perfil local da influencer.
- Validacao: `node --check public/assets/js/admin/admin-videos.js` e `npm run build`.

## Galeria visual da Helena Duarte

- Copiadas 8 imagens da pasta local informada pelo usuario para `public/images/influencers/helena-duarte/`.
- Adicionada galeria de referencias no card da influencer da aba Videos IA.
- Perfil padrao da Heena Duarte passou a ter foto principal e referencias visuais fixas.
- Cache bust atualizado para `admin-videos.js?v=8`.

## Manual da Helena Duarte

- Cadastrado o resumo manual da Helena Duarte no perfil padrao da influencer de MB Negocios.
- Corrigido nome visivel para Helena Duarte, mantendo compatibilidade com o ID interno existente.
- Atualizado cache bust do script de videos para `admin-videos.js?v=9`.

## Video unico e lote por calendario

- Adicionado botao Gerar 1 video na aba Videos IA.
- O lote ganhou campos de primeira publicacao e intervalo em dias.
- Cada roteiro pode exibir postagem planejada e exportar essa data no CSV.
- Cache bust atualizado para `admin-videos.js?v=10`.

## Uso do video e multiplas redes

- Adicionado campo Uso do video na aba Videos IA.
- Canal unico foi substituido por selecao multipla de redes.
- Campos do lote foram separados visualmente como configuracao do lote.
- Cache bust atualizado para `admin-videos.js?v=11`.

## Filtro de redes por uso do video

- Ajustada a lista de redes para mudar conforme Uso do video.
- Conteudo mostra canais organicos, Anuncios mostra canais pagos, Conteudo e anuncios mostra ambos.
- Removidos nomes longos como Reels e Shorts dos canais organicos.
- Cache bust atualizado para `admin-videos.js?v=12`.

## Linha editorial e Kanban de Videos IA

- Adicionada secao Linha editorial na aba Videos IA.
- Criados pilares editoriais e Kanban com Ideias, Roteiro, Producao e Publicado.
- Ideias podem ser aplicadas ao briefing do video.
- Cache bust atualizado para `admin-videos.js?v=13`.

## Remocao de cards auxiliares em Videos IA

- Removidos os cards Fluxo recomendado e Modelos rapidos da lateral da aba Videos IA.
- Cache bust atualizado para `admin-videos.js?v=14`.

## Linha editorial em pagina propria

### Resumo
- Criada a aba Linha editorial no menu principal do admin.
- Removido o Kanban de dentro da tela Videos IA.
- A pagina dedicada usa colunas mais largas e separa pilares editoriais, formulario de nova ideia e board.

### Problemas encontrados e solucoes
- O layout anterior comprimia o Kanban dentro de uma tela que ja tinha formulario, influencer e fila de producao.
- A solucao foi preservar a logica existente e mudar a hierarquia de navegacao, com `initEditorial()` separado no core do admin.

### Validacao
- `node --check public/assets/js/admin/admin-core.js`
- `node --check public/assets/js/admin/admin-videos.js`
- `npm run build`

### Proximos passos
- Validar visualmente a nova tela em producao e avaliar filtros por projeto quando houver mais ideias cadastradas.

## Submenu dentro de Videos IA

### Resumo
- Removido o item Linha editorial do menu principal.
- Criado submenu interno em Videos IA: Criar videos e Linha editorial.
- O Kanban permanece com layout amplo, mas agora dentro do contexto da ferramenta de videos.

### Validacao
- `node --check public/assets/js/admin/admin-core.js`
- `node --check public/assets/js/admin/admin-videos.js`
- `npm run build`

### Proximos passos
- Validar visualmente o fluxo publicado e decidir se novas subabas devem entrar em Videos IA.

## Kanban editorial em largura ampliada

### Resumo
- A subaba Linha editorial agora usa `video-editorial-active` para remover o limite de largura do container.
- Pilares e nova ideia ficaram acima do Kanban.
- O board passou a ocupar a largura total disponivel, com colunas maiores e altura minima mais confortavel.

### Validacao
- `node --check public/assets/js/admin/admin-videos.js`
- `node --check public/assets/js/admin/admin-core.js`
- `npm run build`

### Proximo passo
- Validar visualmente em producao e ajustar largura minima das colunas se o monitor principal ainda exigir scroll.

## Limpeza visual em Criar videos

### Resumo
- Removido o card azul de integracao HeyGen da subaba Criar videos.
- Movida a Fila de producao para baixo do formulario, reduzindo o espaco vazio.
- Removido CSS obsoleto do card removido.

### Validacao
- `node --check public/assets/js/admin/admin-videos.js`
- `node --check public/assets/js/admin/admin-core.js`
- `npm run build`

### Proximo passo
- Conferir em producao a densidade visual da tela e decidir se a fila deve ser compactada.

## Melhor uso lateral em Videos IA

### Resumo
- Removido o limite de largura estreito da tela Videos IA.
- Ajustadas as colunas da subaba Criar videos para usar melhor o espaco horizontal.
- Mantida responsividade para empilhar as colunas em telas menores.

### Validacao
- `node --check public/assets/js/admin/admin-videos.js`
- `node --check public/assets/js/admin/admin-core.js`
- `npm run build`

### Proximo passo
- Conferir visualmente no admin publicado e calibrar proporcoes se ainda houver compressao em alguma area.

## Videos IA - perfis sociais conectados

- Removido o bloco de selecao generica de redes/canais da etapa de criacao de videos.
- Adicionado bloco `Perfis conectados` com acoes para conectar Instagram e Facebook por projeto.
- Criada persistencia local `mb_ai_video_social_accounts_v1` para guardar contas por projeto.
- A fila de videos agora usa os perfis selecionados como `channels/channel` do brief.
- A criacao de video foi bloqueada quando nenhum perfil social esta conectado/selecionado.
- Cache do `admin-videos.js` atualizado de `v=17` para `v=18`.
- Validacoes: `node --check public\assets\js\admin\admin-videos.js`; `npm run build` concluido com sucesso, mantendo apenas avisos antigos de `<img>`.

## Videos IA - conexao real com Meta

- Criadas rotas server-side para OAuth da Meta: `/api/meta/connect` e `/api/meta/callback`.
- Os botoes Conectar Instagram/Facebook agora abrem o login oficial da Meta em popup.
- A callback troca o `code` por token no servidor e consulta `/me/accounts` para retornar paginas do Facebook e Instagram profissional vinculado.
- A tela salva os perfis reais no projeto selecionado e marca a origem como `meta-oauth`.
- Tokens nao sao enviados para o frontend; agendamento real depende de persistencia segura posterior no backend.
- Cache do `admin-videos.js` atualizado para `v=19`.
- Validacoes: `node --check public\assets\js\admin\admin-videos.js`; `npm run build` com sucesso.

## Meta OAuth - ajuste de escopo

- Reduzido o escopo inicial do OAuth para pages_show_list, evitando erro de Invalid Scopes em apps que ainda nao tiveram permissoes de publicacao/Instagram liberadas pela Meta.
- A deteccao de Instagram fica como fallback posterior quando as permissoes forem adicionadas ao app.


## Meta OAuth - fallback de pagina

- A callback agora salva a pagina do Facebook mesmo quando a conexao foi iniciada pelo botao Instagram.
- Quando a Meta nao retorna Instagram, o admin exibe aviso explicando que falta liberar instagram_basic e confirmar vinculo do Instagram profissional com a pagina.
- O listener do popup foi religado durante initVideos() para garantir que o retorno da Meta atualize o card de Perfis conectados.


## Meta OAuth - cache e retorno do popup

- Atualizado cache bust do admin para dmin-videos.js?v=20.
- Adicionado listener global de message para receber o retorno do popup da Meta mesmo antes da reinicializacao da aba.
- Adicionado fallback na Graph API para buscar paginas sem instagram_business_account quando a permissao de Instagram ainda nao estiver liberada.


## Meta OAuth - persistencia local do retorno

- A callback do OAuth agora grava o payload em mb_meta_oauth_result_v1 no localStorage do dominio antes de fechar o popup.
- O admin monitora o popup aberto e consome esse resultado mesmo se o postMessage falhar.
- Mantido cache bust em dmin-videos.js?v=21.


## Sessao 2026-07-13 - Retorno visivel do OAuth Meta em Videos IA

### O que foi feito
- A pagina `/api/meta/callback` deixou de fechar automaticamente o popup da Meta e agora mostra uma confirmacao visivel no dominio `blog.mbfinance.com.br`.
- O callback grava o resultado em `localStorage`, envia `postMessage` para o admin e oferece o botao `Aplicar no admin` como fallback manual.
- O admin Videos IA passou a capturar o retorno da Meta tambem por evento de `storage`, foco da janela e fechamento do popup.
- O script de videos foi versionado para `admin-videos.js?v=22`.

### Arquivos modificados
- `app/api/meta/callback/route.ts`
- `public/assets/js/admin/admin-videos.js`
- `private/blog-admin.html`
- `CONTEXT.md`
- `TODO.md`
- `DECISIONS.md`
- `docs/sessions/2026-07-13.md`
- `CHANGELOG.md`

### Estado atual
- Se a Meta redirecionar corretamente para o callback, o usuario deve ver uma tela de confirmacao do proprio blog antes de voltar ao admin.
- Se a confirmacao nao aparecer depois do botao Entendi da Meta, o problema ainda esta no fluxo/configuracao de redirecionamento da Meta antes de chegar no sistema.

### Validacao
- `node --check public\\assets\\js\\admin\\admin-videos.js` executado com sucesso.
- `npm run build` executado com sucesso, mantendo apenas warnings antigos de `<img>`.

## Sessao 2026-07-13 - Reautorizacao de paginas Meta

### O que foi feito
- A rota `/api/meta/connect` passou a iniciar o OAuth com `auth_type=rerequest` e `return_scopes=true` para forcar a Meta a reavaliar permissoes concedidas.
- A mensagem quando nenhuma pagina e retornada ficou mais operacional, orientando o usuario a entrar com a conta administradora e usar `Editar configuracoes` para liberar todas as paginas desejadas.

### Arquivos modificados
- `app/api/meta/connect/route.ts`
- `app/api/meta/callback/route.ts`
- `CONTEXT.md`
- `TODO.md`
- `DECISIONS.md`
- `docs/sessions/2026-07-13.md`
- `CHANGELOG.md`

### Estado atual
- O sistema salva corretamente as paginas que a Meta devolve.
- Paginas ausentes dependem da selecao/autorizacao feita dentro da propria tela da Meta e das permissoes da conta logada sobre cada pagina.
- Instagram ainda depende de permissao de Instagram no app da Meta e de perfil profissional vinculado a uma pagina.

### Validacao
- `npm run build` executado com sucesso, mantendo apenas warnings antigos de `<img>`.

## Sessao 2026-07-13 - Ativos Meta selecionaveis por projeto

### O que foi feito
- A aba Videos IA deixou de tratar Instagram/Facebook como conexoes separadas por projeto.
- A conexao Meta/Facebook agora importa ativos para uma biblioteca local global do admin.
- Cada projeto passa a selecionar quais ativos importados pertencem a ele: uma pagina do Facebook e, quando disponivel, um Instagram profissional.
- A interface agora mostra grupos separados para `Pagina do Facebook para este projeto` e `Instagram profissional para este projeto`.
- O script de videos foi versionado para `admin-videos.js?v=23`.

### Arquivos modificados
- `private/blog-admin.html`
- `public/assets/js/admin/admin-videos.js`
- `CONTEXT.md`
- `TODO.md`
- `DECISIONS.md`
- `docs/sessions/2026-07-13.md`
- `CHANGELOG.md`

### Estado atual
- O usuario conecta a conta Meta/Facebook uma vez e depois filtra os ativos por projeto.
- Perfis ja conectados anteriormente sao preservados como legado e aparecem na biblioteca de ativos Meta.
- A selecao de ativos por projeto ainda fica em `localStorage` nesta etapa.

### Validacao
- `node --check public\\assets\\js\\admin\\admin-videos.js` executado com sucesso.
- `npm run build` executado com sucesso, mantendo apenas warnings antigos de `<img>`.

## Sessao 2026-07-13 - Callback Meta com retorno automatico

### O que foi feito
- A tela de callback da Meta deixou de ficar parada apos `Conexao Meta finalizada`.
- O callback agora envia o resultado ao admin, tenta fechar o popup automaticamente e redireciona para `/admin#videos` quando o navegador nao permite fechar a janela.
- O botao da tela passou de `Aplicar no admin` para `Voltar para o admin`.

### Arquivos modificados
- `app/api/meta/callback/route.ts`
- `CONTEXT.md`
- `TODO.md`
- `DECISIONS.md`
- `docs/sessions/2026-07-13.md`
- `CHANGELOG.md`

### Estado atual
- A tela de callback pode aparecer rapidamente como confirmacao, mas nao deve prender o usuario nela.
- Build de producao passou, mantendo apenas warnings antigos de `<img>`.

## Sessao 2026-07-13 - Correcao de Instagram no retorno Meta

### O que foi feito
- Corrigido o callback da Meta para nao descartar contas Instagram quando a conexao for iniciada pelo botao `Conectar conta Meta/Facebook`.
- A mensagem de aviso foi ajustada para informar que Instagram ausente depende de perfil profissional, vinculo com pagina retornada e permissao/produto de Instagram no app da Meta.

### Arquivos modificados
- `app/api/meta/callback/route.ts`
- `CONTEXT.md`
- `TODO.md`
- `DECISIONS.md`
- `docs/sessions/2026-07-13.md`
- `CHANGELOG.md`

### Estado atual
- Se a Graph API devolver `instagram_business_account`, o admin passa a importar o Instagram mesmo quando o fluxo comecou pelo Facebook.
- Paginas ausentes continuam dependendo da selecao/autorizacao feita na tela da Meta e das permissoes da conta logada.

### Validacao
- `npm run build` executado com sucesso, mantendo apenas warnings antigos de `<img>`.

## Sessao 2026-07-13 - Remocao de aviso fixo em Videos IA

### O que foi feito
- Removido o aviso fixo `Crie um video unico ou uma sequencia para varios dias` da subaba Criar videos.
- Removido o CSS associado a `video-mode-note` e `video-mode-icon`.

### Arquivos modificados
- `private/blog-admin.html`
- `CONTEXT.md`
- `TODO.md`
- `docs/sessions/2026-07-13.md`
- `CHANGELOG.md`

### Validacao
- Busca por `video-mode-note`/texto do aviso nao retornou ocorrencias.
- `npm run build` executado com sucesso, mantendo apenas warnings antigos de `<img>`.

## Sessao 2026-07-13 - Videos IA como gerador de conteudo

### O que foi feito
- A subaba Criar videos foi remodelada para funcionar como `Gerador de conteudo`, removendo a linguagem principal de lote.
- O campo Produto virou selecao multipla de produtos do conteudo.
- O campo de quantidade passou a representar `Quantas ideias de conteudo` devem ser geradas.
- Foram removidos os campos de primeira publicacao e intervalo entre videos do fluxo principal.
- O briefing foi renomeado para `Filtro criativo e pontos para lapidar`.
- A lista inferior passou a se chamar `Ideias geradas`.
- O script de videos foi versionado para `admin-videos.js?v=24`.

### Arquivos modificados
- `private/blog-admin.html`
- `public/assets/js/admin/admin-videos.js`
- `CONTEXT.md`
- `TODO.md`
- `DECISIONS.md`
- `docs/sessions/2026-07-13.md`
- `CHANGELOG.md`

### Estado atual
- O usuario configura projeto, uso, ativos Meta, multiplos produtos, formato, quantidade de ideias, duracao, tom de voz, influencer, CTA e filtro criativo.
- O botao principal gera a quantidade de ideias indicada, sem apresentar o fluxo como lote/agendamento.

### Validacao
- `node --check public\\assets\\js\\admin\\admin-videos.js` executado com sucesso.
- `npm run build` executado com sucesso, mantendo apenas warnings antigos de `<img>`.

## Sessao 2026-07-13 - Ideias geradas legiveis em Videos IA

### Resumo
- Reorganizada a lista de ideias geradas para exibir cada conteudo em card legivel.
- O roteiro agora aparece em bloco proprio, com expansao por `Ver tudo`.
- Metadados de projeto, produto, destino e tom aparecem em chips acima do roteiro.
- `admin-videos.js` atualizado para `v=25`.

### Problemas encontrados e solucoes
- A visualizacao anterior imitava tabela e truncava o roteiro em uma coluna estreita.
- A solucao foi abandonar a grade tabular para essa lista e priorizar leitura/escaneabilidade.

### Validacao
- `node --check public\\assets\\js\\admin\\admin-videos.js` passou.
- `npm run build` passou com warnings antigos de `<img>`.

### Proximos passos
- Validar em producao a leitura dos roteiros gerados e decidir se a edicao inline do roteiro entra na proxima rodada.
