# Sessao 2026-06-02 - Links curtos MB Finance

## Resumo

- Confirmado em producao que `mbnegocios.com.br/c/teste` encaminha ao blog, enquanto `mbfinance.com.br/c/teste` retorna `404` no Apache.
- Adicionado seletor para gerar links curtos com `mbnegocios.com.br` ou `mbfinance.com.br`.
- A API passou a validar o dominio escolhido por allowlist.
- Preparada regra `.htaccess` da MB Finance para encaminhar `/c/[code]` ao redirecionador central.
- Corrigido o encurtador para validar o destino antes de criar o codigo.
- Liberados os destinos oficiais de WhatsApp usados pelo site.

## Validacao

- `node --check public/assets/js/admin/admin-campaigns.js` executado com sucesso em `blog-pages/`.
- `npm run build` executado com sucesso em `blog-pages/`; permaneceram apenas avisos preexistentes de uso de `<img>`.
- PHP CLI indisponivel nesta maquina; nao foi possivel executar `php -l`.

## Proximos passos

- Publicar `blog-pages/`.
- Enviar `cpanel-upload/public_html/.htaccess` ao CPanel.
- Testar um link real gerado com `mbfinance.com.br`.
- Recriar links antigos que exibem `Destino invalido`, pois foram salvos antes da validacao antecipada.
