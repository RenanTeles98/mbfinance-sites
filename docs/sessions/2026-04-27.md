# Sessao 2026-04-27

## Resumo
- Instalado o Meta Pixel na home principal do site.
- Arquivo alterado: `public/mb-finance-completo.html`.

## Problemas encontrados e solucoes
- Nao havia pixel existente com `fbq` ou ID `1303767088303655`.
- O snippet foi adicionado no `<head>`, conforme solicitado, sem alterar layout, CSS ou fluxo de CTA.

## Decisoes tomadas
- Registrar a instalacao como ADR-014 em `DECISIONS.md`.
- Manter a validacao final para apos deploy, usando Meta Pixel Helper ou Gerenciador de Eventos.

## Proximos passos
- Validar o disparo do evento `PageView` em producao apos deploy.
- Avaliar eventos de conversao para cliques nos CTAs de WhatsApp em uma proxima sessao.

---

## Atualizacao - Pixel global

### Resumo
- O Meta Pixel foi expandido da home para as paginas publicas relevantes do site.
- Foi mantido um unico Pixel: `1303767088303655`.

### Problemas encontrados e solucoes
- Para evitar duplicacao e facilitar manutencao, o snippet inline da home foi substituido por um arquivo central: `public/assets/js/infra/meta-pixel.js`.
- Para as rotas Next.js publicas, foi criado `components/MetaPixel.tsx`.
- O admin legado ficou fora do rastreamento para nao poluir metricas de campanha.

### Decisoes tomadas
- Nao criar pixels diferentes por pagina.
- Aplicar o Pixel nas paginas publicas e preservar o admin sem rastreamento.

### Validacao
- `npm run build` executado com sucesso.
- O build mostrou apenas avisos ja conhecidos de `<img>`/lint, sem erro de compilacao.

### Proximos passos
- Conferir no Meta Pixel Helper apos deploy: home, uma pagina secundaria, `/blog` e um artigo.
- Adicionar eventos de conversao para cliques de WhatsApp em uma proxima etapa.

---

## Atualizacao - Google Ads tag

### Resumo
- Instalacao da Google tag `AW-18112641661` nas paginas publicas relevantes.

### Problemas encontrados e solucoes
- Para evitar duplicacao, a tag foi centralizada em `public/assets/js/infra/google-ads-tag.js` nos HTMLs estaticos.
- Para rotas Next.js publicas, foi criado `components/GoogleAdsTag.tsx`.
- O admin legado ficou fora do rastreamento para nao poluir metricas de campanha.

### Decisoes tomadas
- Nao inserir o snippet inline repetido em todas as paginas.
- Usar uma implementacao compartilhada por camada: HTML estatico e Next.js.

### Validacao
- `npm run build` executado com sucesso.
- O build mostrou apenas avisos ja conhecidos de `<img>`/lint, sem erro de compilacao.

### Proximos passos
- Conferir a Google tag no Tag Assistant apos deploy.
- Criar eventos de conversao para cliques de WhatsApp em uma proxima etapa.

---

## Atualizacao - CSP Google Ads

### Resumo
- Ajustada a CSP em `vercel.json` para resolver alerta do Tag Assistant sobre scripts/conexoes do Google.

### Problemas encontrados e solucoes
- O Tag Assistant encontrou a tag `AW-18112641661`, mas exibiu alerta de CSP bloqueando dominios Google.
- A CSP foi ampliada apenas para os dominios necessarios de Google tag/Ads/Analytics/DoubleClick.

### Validacao
- `vercel.json` validado com `node`.
- `npm run build` executado com sucesso.

### Proximos passos
- Apos deploy, recarregar a pagina sem cache e validar novamente no Tag Assistant.

---

## Atualizacao - CSP Meta Pixel

### Resumo
- Corrigida a CSP para liberar os dominios necessarios do Meta Pixel.

### Problemas encontrados e solucoes
- O Meta Pixel Helper deixou de encontrar o pixel depois do ajuste de CSP para Google Ads.
- A causa provavel era bloqueio de `connect.facebook.net` pelo `script-src`.
- Foram adicionados `connect.facebook.net` e `www.facebook.com` nas diretivas adequadas da CSP.

### Validacao
- `vercel.json` validado com `node`.
- `npm run build` executado com sucesso.

### Proximos passos
- Apos deploy, recarregar a home sem cache e validar no Meta Pixel Helper.

---

## Atualizacao - Meta Pixel inline na home

### Resumo
- A home voltou a usar o snippet oficial inline do Meta Pixel.

### Problemas encontrados e solucoes
- O Meta Pixel Helper continuou sem detectar o pixel na home no navegador do dono.
- A home foi ajustada para o mesmo formato oficial que ja havia sido reconhecido anteriormente.

### Validacao
- A home possui exatamente um `fbq('init', '1303767088303655')`.
- `npm run build` executado com sucesso.

### Proximos passos
- Apos deploy, recarregar a home sem cache e validar no Meta Pixel Helper.

---

## Atualizacao - Meta Pixel inline no blog legado

### Resumo
- A pagina legada `public/pages/blog.html` voltou a usar o snippet oficial inline do Meta Pixel.

### Problemas encontrados e solucoes
- O Meta Pixel Helper nao detectou o pixel no blog legado quando ele carregava via `../assets/js/infra/meta-pixel.js`.
- O snippet oficial foi colocado diretamente no `<head>` dessa pagina, igual ao padrao restaurado na home.

### Decisoes tomadas
- Registrar a mudanca como ADR-020.
- Manter a Google Ads tag externa no blog legado.
- Nao alterar o painel administrativo do blog.

### Validacao
- A pagina possui exatamente um `fbq('init', '1303767088303655')`.
- `npm run build` executado com sucesso.

### Proximos passos
- Apos deploy, recarregar `https://mbfinance-sites.vercel.app/pages/blog.html` sem cache e validar no Meta Pixel Helper.

---

## Atualizacao - Tags na raiz do site

### Resumo
- Corrigida a rota raiz (`/`) para carregar Meta Pixel e Google Ads tag antes do redirecionamento para a home HTML.

### Problemas encontrados e solucoes
- O Google Tag Coverage indicava `mbfinance-sites.vercel.app/` como "Sem tag".
- A raiz usava `redirect()` server-side, entao a URL nao entregava HTML com scripts de mensuracao.
- `app/page.tsx` agora renderiza uma pagina ponte com `GoogleAdsTag`, `MetaPixel` e redirecionamento automatico para `/mb-finance-completo.html`.

### Decisoes tomadas
- Registrar a mudanca como ADR-021.
- Manter a home principal em `public/mb-finance-completo.html`, sem migracao estrutural nesta sessao.

### Validacao
- `npm run build` executado com sucesso.

### Proximos passos
- Apos deploy, testar `https://mbfinance-sites.vercel.app/` no Tag Assistant.
- Aguardar a cobertura do Google atualizar, pois o painel pode demorar a refletir a nova leitura.

---

## Atualizacao - Google Tag Manager

### Resumo
- Instalado o Google Tag Manager `GTM-MDST4NTK` no site publico.

### Problemas encontrados e solucoes
- Os HTMLs publicos nao tinham GTM instalado.
- O snippet foi inserido no topo do `<head>` e o `noscript` logo apos a abertura do `<body>`.
- No Next.js, o GTM foi adicionado em `app/layout.tsx` para cobrir rotas do App Router.

### Decisoes tomadas
- Registrar a instalacao como ADR-022.
- Manter `public/pages/blog-admin.html` sem GTM para nao rastrear o painel administrativo legado.

### Validacao
- 13 HTMLs publicos contem `GTM-MDST4NTK`.
- `public/pages/blog-admin.html` nao contem GTM.
- `npm run build` executado com sucesso.

### Proximos passos
- Apos deploy, validar o container `GTM-MDST4NTK` no Tag Assistant.
