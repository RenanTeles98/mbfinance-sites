# Sessao 2026-08-18 - Padronizacao da marca

## Resumo

- A marca foi padronizada de `mb finance` e `MB Finance` para `Mb Finance` em todo o conteudo do site.
- As submarcas foram padronizadas de `mb negócios`/`MB Negócios` e `mb tributos`/`MB Tributos` para `Mb Negócios` e `Mb Tributos`.
- Corrigida a codificacao UTF-8 dos arquivos publicos alterados para restaurar os acentos do site.
- Foram atualizados textos publicos, metadados SEO, atributos `alt`, mensagens de WhatsApp, e-mails e copias no pacote de deploy CPanel.

## Decisao

- `Mb Finance` passa a ser a grafia institucional unica da marca.

## Proximos passos

- Publicar a atualizacao e validar possiveis caches de CDN, navegador e previews de compartilhamento.
