# Sessao 2026-05-05

## Resumo

- Criada a pasta `blog-pages/` como projeto independente do blog para deploy na Vercel.
- Separadas as rotas Next.js necessarias: `/blog`, `/blog/[slug]`, `/admin`, APIs do blog, newsletter e analytics.
- Configurado dominio canonico do blog como `https://blog.mbfinance.com.br`.
- Criados `app/sitemap.ts`, `app/robots.ts`, `next.config.mjs`, `.env.example` e `vercel.json` dentro de `blog-pages/`.
- Atualizados links do site estatico para apontar para `https://blog.mbfinance.com.br/blog`.
- Substituido `public/pages/blog.html` por uma pagina de redirecionamento com `noindex, follow`.
- Substituida a tela React antiga do `/admin` pela versao atual do painel HTML modular em `blog-pages/public/pages/blog-admin.html`.
- Substituida a pagina publica antiga do `/blog` pela versao visual atual do blog, implementada em `blog-pages/components/BlogIndexClient.tsx`.
- Corrigido mojibake no HTML do admin, restaurando acentos em textos da aba de mÃ©tricas e demais Ã¡reas do painel.
- Preparada persistencia do blog em Supabase Postgres com REST API server-side, SQL de tabela/RLS e guia de configuracao.

## Problemas encontrados e solucoes

- O build inicial falhou porque `components/admin/BlogAdminApp.tsx` foi copiado um nivel abaixo do esperado. O arquivo foi movido para `blog-pages/components/admin/BlogAdminApp.tsx`.
- Depois, a versao React antiga do admin foi removida do fluxo e `/admin` passou a redirecionar para o HTML atual do painel.
- O primeiro build local falhou com `EPERM` ao criar workers do Next.js no sandbox. O build foi repetido com permissao aprovada e passou.
- O HTML do admin estava com texto UTF-8 interpretado como Windows-1252. Foi feita uma recodificacao mecÃ¢nica do arquivo e ajuste pontual do toast "Post salvo".
- A anon key do Supabase nao deve receber permissao de escrita. O painel deve gravar posts via API Next.js usando `SUPABASE_SERVICE_ROLE_KEY` configurada apenas na Vercel.

## Decisoes tomadas

- O blog oficial sera `https://blog.mbfinance.com.br`, hospedado na Vercel.
- O Cpanel continua servindo o site institucional estatico.
- A URL antiga `/pages/blog.html` nao deve ser indexada; ela redireciona para o blog oficial.
- O blog separado tera sitemap proprio e deve ser cadastrado separadamente no Google Search Console.

## Validacao

- `next build` executado em `blog-pages/` com sucesso.
- Restaram apenas avisos nao bloqueantes de `<img>` em componentes.

## Proximos passos

- Criar/importar projeto na Vercel apontando para `blog-pages/`.
- Configurar `blog.mbfinance.com.br` na Vercel e DNS.
- Cadastrar variaveis de ambiente listadas em `blog-pages/.env.example`.
- Executar `blog-pages/supabase-blog.sql` no SQL Editor do Supabase.
- Enviar `https://blog.mbfinance.com.br/sitemap.xml` no Google Search Console.

## Complemento - conexao Supabase local

- Ajustado `blog-pages/lib/blog-store.ts` para considerar o Supabase configurado quando houver `SUPABASE_URL` e `SUPABASE_SERVICE_ROLE_KEY` ou `SUPABASE_ANON_KEY`.
- Ajustado `blog-pages/app/api/blog/sync/route.ts` para usar a camada oficial `readBlogPosts`/`writeBlogPosts`, removendo a dependencia direta de Redis nesse endpoint.
- Validado que o Supabase responde via HTTPS com as chaves do `blog-pages/.env.local`.
- A API inicialmente retornou `PGRST205`, confirmando conexao ao Supabase antes da tabela existir.
- Depois da execucao de `blog-pages/supabase-blog.sql` no Supabase, `public.blog_posts` passou a responder com status `200`.
- Os 5 posts de `blog-pages/content/blog-posts.json` foram inseridos/atualizados em `public.blog_posts`.
- `npm run build` executado em `blog-pages/` com sucesso; restaram apenas avisos preexistentes de `<img>`.

## Complemento - erro de pasta com espaco na Vercel

- O deploy da Vercel falhou com `A Serverless Function has an invalid name: "Blog pages/___next_launcher.cjs"`.
- Causa: o caminho do Root Directory continha espaco (`Blog pages`), que entrou no nome interno da serverless function.
- Criada a pasta `blog-pages/` como copia do projeto do blog, sem `node_modules` e sem `.next`.
- `npm run build` executado em `blog-pages/` com sucesso.

Proximo passo tecnico: publicar `blog-pages/` no GitHub, ajustar o Root Directory na Vercel para `blog-pages`, cadastrar as mesmas variaveis no projeto da Vercel e validar uma publicacao pelo painel `/admin`.

## Complemento - admin carregando posts

- O painel em producao exibia "Somente local" porque `mb_blog_api_base` ainda nao existia no `localStorage` do novo dominio.
- Corrigido `blog-pages/public/assets/js/admin/admin-blog.js` para usar `window.location.origin` como API base padrao.
- `npm run build` executado em `blog-pages/` com sucesso.

Proximo passo tecnico: redeployar na Vercel e recarregar `/admin` com cache limpo.

## Complemento - publicacao pelo admin

- A API de producao foi testada diretamente: `GET /api/blog/posts` retornou 5 posts e `PUT /api/blog/posts` retornou `200`, confirmando que Supabase e service role estavam funcionais.
- O painel ainda podia salvar localmente com aparencia de sucesso quando a sincronizacao falhava.
- Corrigido `blog-pages/public/assets/js/admin/admin-blog.js` para carregar posts com token admin, exigir sucesso do PUT antes de mostrar "Post salvo e publicado" e alertar quando salvar apenas no navegador.
- Corrigido `blog-pages/app/api/blog/posts/route.ts` para retornar erro JSON em falhas de escrita.
- `npm run build` executado em `blog-pages/` com sucesso.

Proximo passo tecnico: redeployar na Vercel e testar a criacao de um post publicado com data/hora atual ou passada.

